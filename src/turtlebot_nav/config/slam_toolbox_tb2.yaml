slam_toolbox:
  ros__parameters:
    use_sim_time: false

    mode: mapping

    map_frame: map
    odom_frame: odom
    base_frame: base_footprint
    scan_topic: /scan

    minimum_laser_range: 0.1
    maximum_laser_range: 18

    transform_timeout: 0.5
    tf_buffer_duration: 30.0